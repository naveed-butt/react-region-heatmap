# Normalise line endings in the repository to LF regardless of contributor OS.
# CI runs on Linux and prettier checks against LF, so CRLF in the index would
# fail the format gate for Windows contributors only.
* text=auto eol=lf

# Generated by npm; do not diff it.
package-lock.json linguist-generated=true -diff
